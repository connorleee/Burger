<div>
	{{burger_name}}

	<button class="btn btn-primary float-right devour" data-id="{{id}}" data-devourState="{{devoured}}">Devour!</button>

	{{!-- <button class="delete-cat" data-id="{{this.id}}">DELETE!</button> --}}
</div>
